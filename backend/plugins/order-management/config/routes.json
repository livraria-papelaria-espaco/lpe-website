{
  "routes": [
    {
      "method": "POST",
      "path": "/order/nextstep/:id",
      "handler": "OrderManagement.nextstep",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/order/updaterestockcount/:id",
      "handler": "OrderManagement.updateRestockCount",
      "config": {
        "policies": []
      }
    }
  ]
}
