{
  "host": "localhost",
  "port": "${process.env.PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": true,
    "ssl": true,
    "host": "${process.env.PROXY_HOST || 'localhost'}",
    "port": "${process.env.PROXY_PORT || 1337}"
  },
  "cron": {
    "enabled": true
  },
  "admin": {
    "autoOpen": false
  }
}
