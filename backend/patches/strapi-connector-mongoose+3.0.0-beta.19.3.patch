index 1b47a17..0feed79
--- a/node_modules/strapi-connector-mongoose/lib/queries.js
+++ b/node_modules/strapi-connector-mongoose/lib/queries.js
@@ -550,6 +550,7 @@ module.exports = ({ model, modelKey, strapi }) => {
     const filters = modelUtils.convertParams(modelKey, params);
 
     const $or = buildSearchOr(model, params._q);
+    if ($or.length === 0) return Promise.resolve([]);
 
     return model
       .find({ $or })
@@ -564,6 +565,7 @@ module.exports = ({ model, modelKey, strapi }) => {
 
   function countSearch(params) {
     const $or = buildSearchOr(model, params._q);
+    if ($or.length === 0) return Promise.resolve(0);
     return model.find({ $or }).countDocuments();
   }
 
@@ -593,6 +595,7 @@ const buildSearchOr = (model, query) => {
       case 'string':
       case 'text':
       case 'password':
+      case 'uid':
         return acc.concat({ [curr]: { $regex: query, $options: 'i' } });
       case 'boolean':
         if (query === 'true' || query === 'false') {
